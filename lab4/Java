Структура: Бинарное дерево поиска (BST)

Определение:
Бинарное дерево поиска — это дерево, в котором каждый узел имеет не более двух потомков (левый и правый), и для любого узла выполняется условие: все значения в левом поддереве меньше, а в правом поддереве больше значения самого узла.

Реализация в Java:
Ручная реализация через класс Node
TreeMap из java.util — реализация красно-черного дерева

Структура: Граф (список смежности)
  
Определение:
Граф — это набор вершин и рёбер, соединяющих пары вершин. Список смежности — это способ представления графа, где для каждой вершины хранится список смежных с ней вершин.

Реализация в Java:
List<List<Integer>> graph = new ArrayList<>() - список списков, где индекс это номер вершины

Структура: Граф (матрица смежности)

Определение:
Матрица смежности — это способ представления графа в виде квадратной матрицы, где элемент matrix[i][j] указывает на наличие (и вес) ребра из вершины i в вершину j.

Реализация в Java:
Двумерный массив: int[][] matrix = new int[n][n]
