Рабочий код на языке программирования C++:

#include <iostream> // Подключаем библиотеку ввода-вывода

// Функция для вывода массива на экран
void printArray(int arr[], int size) {
    for (int i = 0; i < size; ++i) { // Проходим по каждому элементу массива
        std::cout << arr[i] << " ";   // Выводим каждый элемент
    }
    std::cout << "\n";                // Перевод строки после завершения вывода массива
}

// Основная функция сортировки выбором
void selectionSort(int arr[], int n) {
    int minIndex;                     // Переменная для хранения индекса минимального элемента

    // Внешний цикл проходит по массиву слева направо
    for (int i = 0; i < n - 1; ++i) { // Выполняем итерации до предпоследнего элемента

        // Сначала считаем первый нерассмотренный элемент минимальным
        minIndex = i;

        // Внутренний цикл ищет минимальный элемент среди оставшихся элементов справа
        for (int j = i + 1; j < n; ++j) {
            if (arr[j] < arr[minIndex]) { // Если нашли меньший элемент, обновляем индекс минимума
                minIndex = j;
            }
        }

        // Меняем местами найденный минимум с первым элементом текущего интервала
        if (minIndex != i) {              // Проверка необходима, чтобы избежать ненужных обменов
            std::swap(arr[i], arr[minIndex]); // Используем стандартную функцию swap для обмена значений
        }
    }
}

// Главная функция программы
int main() {
    int arr[] = {64, 25, 12, 22, 11}; // Создаем тестовый массив
    int n = sizeof(arr)/sizeof(arr[0]); // Вычисляем размер массива путем деления общего размера на размер одного элемента

    std::cout << "IshodnMass:\n";
    printArray(arr, n);                 // Вывод исходного массива перед сортировкой

    selectionSort(arr, n);              // Вызываем функцию сортировки выбором

    std::cout << "SortedMass:\n";
    printArray(arr, n);                 // Вывод отсортированного массива

    return 0;                           // Завершаем программу успешно
}

Результат работы программы:
IshodnMass:
64 25 12 22 11
SortedMass:
11 12 22 25 64
