Рабочий код на языке программирования C++:

#include <iostream>  // Заголовочный файл для работы с потоками ввода-вывода

// Функция для вывода массива на экран
void printArray(const int* arr, const int size) {
    for (int i = 0; i < size; ++i) {           // Проходим по каждому элементу массива
        std::cout << arr[i] << " ";            // Выводим элемент на экран
    }
    std::cout << "\n";                         // Новый строковый разрыв после вывода массива
}

// Основная функция сортировки вставками
void insertionSort(int arr[], int n) {
    for (int i = 1; i < n; ++i) {              // Проходим по массиву, начиная со второго элемента
        int key = arr[i];                      // Сохраняем текущий элемент
        int j = i - 1;                         // Индекс предыдущей позиции
        
        // Смещаем элементы вправо, пока не найдём подходящее место для вставки
        while (j >= 0 && arr[j] > key) {
            arr[j + 1] = arr[j];              // Сдвигаем больший элемент вправо
            j--;                               // Двигаемся назад по массиву
        }
        
        arr[j + 1] = key;                     // Вставляем текущий элемент на подходящую позицию
    }
}

// Главная функция программы
int main() {
    int arr[] = {64, 34, 25, 12, 22, 11, 90}; // Создаём тестовый массив
    int n = sizeof(arr) / sizeof(arr[0]);      // Вычисляем размер массива
    
    std::cout << "Исходный массив:\n";
    printArray(arr, n);                        // Выводим исходный массив
    
    insertionSort(arr, n);                     // Сортируем массив методом вставок
    
    std::cout << "Отсортированный массив:\n";
    printArray(arr, n);                        // Выводим отсортированный массив
    
    return 0;                                  // Выход из программы
}

Результат работы кода:
IshodnMass:
64 34 25 12 22 11 90
SortedMass:
11 12 22 25 34 64 90
