Рабочий код на языке программирования C++:

#include <iostream>     // Включаем библиотеку для работы с вводом-выводом

// Вспомогательная функция для вывода массива на экран
void printArray(int arr[], int size) {
    for (int i = 0; i < size; ++i) {       // Проходим по каждому элементу массива
        std::cout << arr[i] << " ";        // Выводим каждый элемент
    }
    std::cout << "\n";                    // Добавляем перенос строки после вывода массива
}

// Основная функция сортировки пузырьком
void bubbleSort(int arr[], int n) {
    bool swapped;                         // Флаг, показывающий, были ли произведены изменения
    
    // Основной цикл, выполняемый n-1 раз (количество возможных сдвигов)
    for (int i = 0; i < n - 1; ++i) {
        swapped = false;                   // Очищаем флаг перед каждым новым проходом
        
        // Вложенный цикл, который сравнивает соседние элементы
        for (int j = 0; j < n - i - 1; ++j) {
            
            // Если текущий элемент больше следующего, производим обмен
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]); // Стандартная библиотека для обмена значений
                swapped = true;               // Устанавливаем флаг, если произошло изменение
            }
        }
        
        // Если на данном этапе изменений не было, значит массив уже отсортирован
        if (!swapped) break;                  // Ранний выход из основного цикла
    }
}

// Точка входа в программу
int main() {
    int arr[] = {64, 34, 25, 12, 22, 11, 90}; // Тестовый массив
    int n = sizeof(arr)/sizeof(arr[0]);      // Вычисление длины массива
    
    std::cout << "Исходный массив:\n";
    printArray(arr, n);                      // Вывод исходного массива
    
    bubbleSort(arr, n);                      // Запуск процедуры сортировки пузырьком
    
    std::cout << "Отсортированный массив:\n";
    printArray(arr, n);                      // Вывод отсортированного массива
    
    return 0;                               // Нормальное завершение программы
}


Результат работы программы:
IshodnMass:
64 34 25 12 22 11 90
SortedMass:
11 12 22 25 34 64 90
